# OHINO Core Package

Core engine, service locator, storage and memory primitives used across OHINO-S.

This package provides a lightweight bootstrap to initialize shared services and lifecycle management for modules and the app.
